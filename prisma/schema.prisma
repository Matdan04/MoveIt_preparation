// Skeleton only. Domain models are added in Step 1.
generator client {
  provider = "prisma-client-js"
}

datasource db {
  // Prisma 7: no `url` here. The connection string lives in prisma.config.ts
  // for the CLI and in lib/db.ts (driver adapter) at runtime.
  provider = "postgresql"
}
